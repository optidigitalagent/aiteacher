# Backend origin — required for REST API calls and WebSocket URL derivation
VITE_API_URL=https://aiteacher-production-bda7.up.railway.app

# WebSocket URL override (optional — auto-derived from VITE_API_URL when not set)
# https → wss, http → ws conversion is automatic.
# VITE_WS_URL=wss://aiteacher-production-bda7.up.railway.app
